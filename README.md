# 🦖 InstallProtect v10 (Anti Tautan Server)

Script ini memasang proteksi agar server tidak bisa dihapus sembarangan di panel Pterodactyl.

## 🚀 Install
Jalankan sebagai root:

```bash
curl -fsSL https://raw.githubusercontent.com/aretaazhar05/install-v10.sh-by-official/main/install-v10.sh | sudo bash
